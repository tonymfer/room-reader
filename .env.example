# room-reader works without these. Public anon values only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional server-side API route for GPT-powered persona reactions.
# Do not expose in client code and do not commit real values.
OPENAI_API_KEY=
# Optional override; defaults to gpt-4o-mini.
OPENAI_MODEL=
